---
layout: base.njk
title: "Installations | Huey B"
nav: installations
---
    <div class="projects-layout">
      <nav class="project-index" aria-label="Jump to piece">
        <h2 class="project-index-title">Installations</h2>
        <ul>
          {% for project in collections.installations %}
          <li>
            <a href="#{{ project.fileSlug }}" class="project-index-link">
              <span class="scramble-target project-scramble" aria-hidden="true"></span>
              <span class="visually-hidden">{{ project.data.title }}</span>
            </a>
          </li>
          {% endfor %}
        </ul>
      </nav>

      <div class="projects-scroll">
        {% for project in collections.installations %}
        <a href="/installations/{{ project.fileSlug }}/" id="{{ project.fileSlug }}" class="project-panel">
          {% if project.data.cover %}
          <div class="project-panel-image">
            <img src="{{ project.data.cover }}" alt="" loading="lazy">
          </div>
          {% endif %}
          <div class="project-panel-overlay">
            <h3>
              <span class="scramble-target project-scramble" aria-hidden="true"></span>
              <span class="visually-hidden">{{ project.data.title }}</span>
            </h3>
            {% if project.data.caption %}
            <p>
              <span class="scramble-target project-scramble" aria-hidden="true"></span>
              <span class="visually-hidden">{{ project.data.caption }}</span>
            </p>
            {% endif %}
          </div>
        </a>
        {% endfor %}
      </div>
    </div>
